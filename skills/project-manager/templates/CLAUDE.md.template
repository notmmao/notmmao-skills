# CLAUDE.md

> 本项目使用 **项目管理框架 v2.0**，配置位于 `.pm/CONFIG/PROJECT_CONFIG.yaml`

## 核心文档

### `.pm/CONFIG/PROJECT_CONFIG.yaml`
- 项目元数据配置（项目名、角色、分支策略等）
- 便于迁移到其他项目

### `.pm/STATUS/` 目录（状态管理）
- `current.md` - 当前阶段、进度、阻塞问题
- `tasks.md` - 任务列表（待办/进行中/已完成）
- `decisions.md` - 决策记录表
- `branches.md` - 分支管理策略

### `.pm/ROADMAP.md`
- 需求收集 → 计划制定 → 测试验证 → 结果分析 的循环路线图
- 里程碑和阶段性目标

### `.pm/LOG.md`
- 按日期分组的操作记录
- 记录重要操作和决策

### 需求分析区（`.pm/需求/` 目录）
- 原始需求文档
- 需求分析结果
- 测试用例设计

### 输出区（`.pm/输出/` 目录）
- 测试报告
- 分析结果
- 验证结论

## 工作流程

```
需求收集 → 分析 → 计划 → 测试 → 验证 → 反馈 → 需求收集
                ↑________________________|
```

## 指令系统

当用户输入以下任何指令时，必须严格按照 `.pm/工作流/指令手册.md` 中定义的行为执行。

执行流程：
1. 检测到指令 → 立即读取 `.pm/工作流/指令手册.md`
2. 找到对应定义 → 严格按照手册中的行为执行
3. 执行完成 → 更新对应的状态文件（.pm/STATUS/*.md）

### 指令索引

| 指令 | 用途 | 读取路径 |
|------|------|----------|
| `继续` | 执行下一个待办任务 | .pm/STATUS/tasks.md |
| `需求 [描述]` | 提交新需求 | 创建到 .pm/需求/待分析.md |
| `分析 [路径]` | 分析代码/文档 | 指定路径 |
| `消化 [路径]` | 处理原始未整理内容 | .pm/需求/待消化/ |
| `计划` | 查看待办任务列表 | .pm/STATUS/tasks.md |
| `状态` | 查看当前项目状态 | .pm/STATUS/current.md |
| `测试 [用例]` | 执行测试用例 | 输出到 .pm/输出/ |
| `日报` | 生成工作报告 | .pm/STATUS/*.md + .pm/LOG.md |
| `摘要 [文档]` | 生成摘要 | 输出到 原路径_摘要.md |
| `同步 [URL]` | 同步飞书文档 | .pm/scripts/feishu_sync.py |
| `发布 [文档]` | 生成 PDF | .pm/scripts/md2pdf.py |

## 文档更新时机

- 任务状态变化 → 更新 `.pm/STATUS/tasks.md`
- 决策做出 → 更新 `.pm/STATUS/decisions.md`
- 每完成一个阶段 → 更新 `.pm/STATUS/current.md`
- 每个大循环完成 → 更新 `.pm/ROADMAP.md`
- 每日工作结束 → 追加 `.pm/LOG.md`
- 测试执行完成 → 输出到 `.pm/输出/` 目录

---

## 开发规范

### [自定义规范]

在此处添加项目特定的开发规范...
